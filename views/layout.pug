mixin tags(...items)
  .project__tags
    each item in items
      span ##{item}

mixin sectionHeader(text)
  .is-flex
    hr.rule--short
    h1.content__header #{text}
    hr.rule

mixin navLinks(...links)
  each link in links
    - const url = `/#${link.toLowerCase()}`;
    a.navbar__list-item.link(href=url) #{link.toUpperCase()}

// Thanks for visiting my website!
doctype html
html(lang="en-us")
  head
    meta(charset="utf-8")
    title Chanse Taylor's Home Page
    link(rel='stylesheet', href='/stylesheets/main.css')
    meta(name="viewport" content="width=device-width, initial-scale=1")
  body.no-margins
    .wrapper
      header.header
        nav.navbar.is-flex
          hr.rule--short
          img.navbar__icon(alt="Nav Icon" src="https://via.placeholder.com/50")
          hr.rule
          .navbar__list.no-margins.is-flex
            +navLinks("About", "Projects", "Contact")
            a.navbar__list-item--special(href="/resume") RESUMÉ
          hr.rule--short
      block content
      block footer
        footer#contact.footer.is-flex
          .footer__credit
            p Webpage developed and designed by Chanse Taylor!
          .footer__contact.is-flex
            a.footer__link.link(href="https://github.com/ChanseNTaylor") GITHUB
            a.footer__link.link(href="mailto:chansen.taylor@gmail.com") GMAIL
